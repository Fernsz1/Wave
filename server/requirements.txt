Django>=5.0,<5.2
djangorestframework>=3.15
paho-mqtt>=2.0
pyserial>=3.5
# Database config: .env loading + DATABASE_URL parsing (+ Postgres driver).
# Leave DATABASE_URL unset to use the bundled SQLite (offline/dev default).
python-dotenv>=1.0
dj-database-url>=2.1
psycopg[binary]>=3.1
# AI lesson/quiz generation (LangGraph multi-agent pipeline + single-shot Gemini).
# Requires GOOGLE_API_KEY (agents) / GEMINI_API_KEY (single-shot) at runtime.
pydantic>=2.0
langgraph>=1.0
langchain-core>=1.0
langchain-google-genai>=4.0
google-genai>=2.0
pytest>=8.0
pytest-django>=4.8
